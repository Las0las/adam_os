import { JobIntakeStudio } from "@/components/lawrence/job-intake/JobIntakeStudio";

export const metadata = {
  title: "LAWRENCE — Job Intake Studio",
  description: "The first implementation of LDS-001, the LAWRENCE / Aberdeen Design System.",
};

export default function JobIntakePage() {
  return <JobIntakeStudio />;
}
