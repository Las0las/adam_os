import { RuntimeConsole } from "@/components/runtime-console/RuntimeConsole";

export const metadata = {
  title: "LAWRENCE · Enterprise Object Runtime",
  description: "One prompt. Every object. Governed execution. The Aberdeen Recruiting command center.",
};

export default function RuntimeConsolePage() {
  return <RuntimeConsole />;
}
